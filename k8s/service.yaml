apiVersion: v1
kind: Service
metadata:
  name: skrgus-web
  labels:
    app: skrgus-web
spec:
  type: ClusterIP
  selector:
    app: skrgus-web
  ports:
    - name: http
      port: 80
      targetPort: http
